@page
@model AT_CSharp2_Oficial.Pages.Cidades.IndexModel

@{
	ViewData["Title"] = "Index";
}

<p>
	<a asp-page="Create">Criar Novo</a>
</p>
<table class="table">
	<thead>
		<tr>
			<th>
				@Html.DisplayNameFor(model => model.CidadeDestino[0].Nome)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.CidadeDestino[0].PaisDestino)
			</th>
			<th></th>
		</tr>
	</thead>
	<tbody>
		@foreach (var item in Model.CidadeDestino)
		{
			<tr>
				<td>
					@Html.DisplayFor(modelItem => item.Nome)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.PaisDestino.Id)
				</td>
				<td>
					<a asp-page="./Edit" asp-route-id="@item.Id">Editar</a> |
					<a asp-page="./Details" asp-route-id="@item.Id">Detalhes</a> |
					<a asp-page="./Delete" asp-route-id="@item.Id">Apagar</a>
				</td>
			</tr>
		}
	</tbody>
</table>

<a asp-page="../Pacotes/Index">Voltar para página inicial</a>
